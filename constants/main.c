#include <stdio.h>
#include <stdlib.h>

int main()
{
    const int NUM = 5;          // constant variables are normally done with all cap letters in name.
    printf("%d\n", NUM);

    // num = 8;
    printf("%d\n", NUM);

    return 0;
}